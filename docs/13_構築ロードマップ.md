# 13. 構築ロードマップと現在地

> **本章の役割**：確定事項、完了済み作業、次に行う作業、未確定リスクを1か所で確認する。
> 詳細な読み順は [README](../README.md)、RAMScope VIの作成手順は [10B](./10B_RAMScope_VI作成手順_STEP3_STEP4詳細.md) を正とする。

**最終整理日：2026-07-14**

---

## 13.1 確定済みの方針

| 項目 | 現在の決定 |
|------|------------|
| LabVIEW | 64bit版 |
| RAMScope実装 | RAMScopeVP API 64bit版をCLFNで直接呼ぶ |
| RAMScope接続 | USB3.0 |
| API DLL | `RAMScopeVP_API_x64.dll` |
| DLL層 | **1関数 = 1薄い`RS_DLL_*`ラッパVI** |
| 公開層 | **1イベント = 1`RAMScope_*`公開API VI** |
| 単体確認 | `PoC_RAMScope_Main.vi`でTestStandなしの通し確認を先に行う |
| CAN | RAM計測PoC後に方式を確定し、採用方式だけを実装する |
| TestStand | RAM/CAN単体PoC後に公開APIを呼ぶ |
| エラー処理 | CLFN error cluster + API ReturnCode変換 + Status/TestError |
| 安全停止 | TestStand Cleanupで高圧停止を最優先 |

### 現在のRAMScope実装ルート

```text
10A：環境準備・DLL疎通
  ↓
薄いDLLラッパを作成
  ↓
Parser・共通部品を作成
  ↓
公開APIを作成
  ↓
PoC_RAMScope_Main.viでRAM計測単体試験
  ↓
CAN方式を確定し、CAN単体PoC
  ↓
11：TestStand組み込み
  ↓
12：異常系・Cleanup検証
```

---

## 13.2 フェーズと進捗

### フェーズ0：方式・環境決定

- [x] LabVIEW 64bitを使用
- [x] RAMScopeVP API 64bit版を使用
- [x] RAMScopeはUSB3.0接続
- [x] API DLLパスを確定
- [x] ヘッダとベンダーサンプルを入手
- [x] Visual C++ 2013 x64ランタイムの役割を整理
- [x] x86版`120`系DLL混在によるエラー193を解消
- [x] PowerShellでDLLロードと関数解決を確認
- [ ] 試験用PCで同じ環境構築を再現

### フェーズ1：共通LabVIEW基盤

- [ ] `Status.ctl`を完成
- [ ] `TestError.ctl`をU32コード対応で完成
- [ ] `Error_To_TestStatus.vi`を完成
- [ ] `RAMScope_Code_To_Error.vi`を完成
- [ ] DLLラッパ用VIテンプレートを完成
- [ ] 公開API用VIテンプレートを完成

### フェーズ2：薄いDLLラッパ

#### PoC済みの元データ

- [x] `RAMScopeGT150DeviceInit`のCLFN設定を確認
- [x] PowerShellからDeviceInitを実呼び出し
- [x] 実機未接続時にクラッシュせずReturnCodeを取得

#### 作成するラッパ

- [ ] `RS_DLL_GT150DeviceInit.vi`
- [ ] `RS_DLL_GT150DeviceExit.vi`
- [ ] `RS_DLL_GT150AllInit.vi`
- [ ] `RS_DLL_GT150GetSysInfo.vi`
- [ ] `RS_DLL_GT150PGT_SetMdlConfig.vi`
- [ ] `RS_DLL_GT170SetMeasCond.vi`
- [ ] `RS_DLL_GT170SetMeasCh.vi`
- [ ] `RS_DLL_GT150SetLoggingInfo.vi`
- [ ] `RS_DLL_GT150MeasStart.vi`
- [ ] `RS_DLL_GT150GetBufferData.vi`
- [ ] `RS_DLL_GT150ReleaseBufferData.vi`
- [ ] `RS_DLL_GT150MeasStop.vi`

各ラッパは1個のCLFNだけを持ち、Parser、Status生成、複数関数の順序制御を含めない。

### フェーズ3：Parser・構造体生成

- [ ] `Parse_SYSINFO_Array.vi`
- [ ] `RAMScope_Parse_Buffer.vi`
- [ ] `RAMScope_Channel.ctl`
- [ ] `MEASINFO_170`生成処理
- [ ] `CHINFO_170`配列生成処理
- [ ] `LOGINFO`生成処理
- [ ] ダミーバイト列によるParser単体試験

### フェーズ4：公開API

- [ ] `RAMScope_Connect.vi`
  - `RS_DLL_GT150DeviceInit.vi`を使用
  - UnitNum / kindを出力
- [ ] `RAMScope_Init.vi`
  - AllInit → GetSysInfo → Parse SYSINFO → PGT_SetMdlConfig
- [ ] `RAMScope_Set_Cond.vi`
  - SetMeasCond → SetMeasCh → SetLoggingInfo
- [ ] `RAMScope_Log_Start.vi`
- [ ] `RAMScope_Read.vi`
  - GetBufferData → Parse Buffer
- [ ] `RAMScope_Release.vi`
  - 実験用。必須性確定まではReadへ内包しない
- [ ] `RAMScope_Log_Stop.vi`
- [ ] `RAMScope_Close.vi`
  - 前段エラーがあってもDeviceExitを実行

公開APIの最後で`Error_To_TestStatus.vi`を呼び、Status / TestError / error outを出力する。

### フェーズ5：RAMScope RAM計測の最小PoC

#### 作成物

- [ ] `PoC_RAMScope_Main.vi`

#### ベースラインフロー

```text
Connect
  → Init
  → Set_Cond
  → Log_Start
  → Wait
  → Read
  → Log_Stop
  → Close
```

#### 確認項目

- [ ] GT170接続時にDeviceInitが成功する
- [ ] UnitNum / kindを取得する
- [ ] AllInitが成功する
- [ ] GetSysInfoからMdlNo_RAM / MdlNo_CAN / Endianを取得する
- [ ] PGT設定とSlotErrを確認する
- [ ] SetMeasCond / SetMeasCh / SetLoggingInfoが成功する
- [ ] MeasStart / GetBufferData / MeasStopが通る
- [ ] 既知RAM変数と解析値が一致する
- [ ] Raw Buffer / DataNum / LostDataNum / Timestampを保存する
- [ ] 正常・異常の両方でDeviceExitまで実行する
- [ ] 複数回の再接続・再測定ができる
- [ ] ReleaseBufferDataあり／なし／位置違いを比較する

### フェーズ6：RAMScope CANまたは採用CAN方式の単体PoC

RAM計測PoC完了後に着手する。

- [ ] CAN方式を決定する
  - CANalyzer COM / ActiveX
  - NI-XNET
  - メーカーUSB-CAN
  - RAMScope GT170 CAN
- [ ] 採用方式の薄いラッパを作成
- [ ] 採用方式の公開APIを作成
- [ ] CAN単体PoC VIを作成
- [ ] Tx / Rx / Timestamp / エラーを確認
- [ ] 同一CAN IDの二重送信がないことを確認
- [ ] RAM計測とCANを組み合わせた単体Flowを確認

CAN詳細は [09](./09_CAN通信の実装.md) を正とする。

### フェーズ7：TestStand組み込み

RAM計測PoCと採用CAN方式の単体PoCが完了してから着手する。

- [ ] LabVIEW Adapterを64bit構成へ設定
- [ ] TestStandから`RS_DLL_*`を直接呼ばない
- [ ] SetupへConnect / Init / Set_Condを配置
- [ ] MainへStart / Read / Stopを配置
- [ ] CleanupへCloseを配置
- [ ] ReadのLoop / Wait / タイムアウトをTestStand側で管理
- [ ] 試験条件をFileGlobals / Parametersへ定義
- [ ] Property Loaderで外部条件を読み込む
- [ ] ステップ結果をレポートへ保存
- [ ] 必要な場合だけ`TS_RAMScope_*.vi`アダプタを追加する

### フェーズ8：異常系・安全試験

- [ ] 高圧が最初にOFFになる
- [ ] 異常データを保存する
- [ ] DUT低圧停止後にRAMScopeを終了する
- [ ] RAMScope計測中エラーでStop / Closeが動く
- [ ] Cleanup中エラーでも残りの停止処理を継続する
- [ ] 元エラーとCleanupエラーを追跡できる

### フェーズ9：展開・再利用

- [ ] 試験用PCへ正規インストーラとRuntimeを導入
- [ ] 開発PCと同じDLL疎通結果を得る
- [ ] 条件変更だけで再試験できる
- [ ] サブシーケンスの並び替えで別試験へ流用できる
- [ ] 長時間試験・再起動・連続試験を確認

---

## 13.3 直近の作業順

現在は次の順で進める。

```text
1. RAMScope_Code_To_Error.vi
2. 作成済みDeviceInit CLFNをRS_DLL_GT150DeviceInit.viへ切り出す
3. 単純なDLLラッパをテンプレートから作る
   DeviceExit → AllInit → MeasStart → MeasStop → ReleaseBufferData
4. 配列・構造体を使うDLLラッパを作る
   GetSysInfo → PGT_SetMdlConfig → SetMeasCond → SetMeasCh → SetLoggingInfo → GetBufferData
5. Parse_SYSINFO_Array.vi / RAMScope_Parse_Buffer.vi
6. RAMScope_Connect.vi / Init.vi / Set_Cond.vi / Start.vi / Read.vi / Stop.vi / Close.vi
7. PoC_RAMScope_Main.vi
8. GT170実機でRAM計測単体PoC
9. CAN方式決定・CAN単体PoC
10. TestStand組み込み
11. 異常系試験
```

---

## 13.4 マイルストーン

| マイルストーン | 完了条件 | 状態 |
|----------------|----------|------|
| M0 方針確定 | 64bit API直呼び方式と資料の正本を確定 | **完了** |
| M1 DLL疎通 | x64 DLLロード、関数解決、DeviceInit実呼び出し | **完了** |
| M2 DLLラッパ | 最小PoCに必要な`RS_DLL_*`が完成 | 未着手 |
| M3 公開API | ConnectからCloseまでの`RAMScope_*`が完成 | 未着手 |
| M4 RAM PoC | GT170で既知RAM値を取得し、終了まで完走 | 未着手 |
| M5 CAN PoC | 採用CAN方式のTx/Rx単体確認が完了 | 未着手 |
| M6 TestStand | 1条件で自動試験が通る | 未着手 |
| M7 異常系 | Cleanup、安全停止、データ退避が機能 | 未着手 |
| M8 横展開 | 条件変更・別PC・別試験で再利用 | 未着手 |

---

## 13.5 未確定事項

| 項目 | 状態 | 確定方法 |
|------|------|----------|
| DeviceInit正常値 | 実機未確認 | GT170実機接続 |
| `0x30100001`の正式意味 | 未確認 | APIエラー表またはベンダー確認 |
| AllInit以降の通し動作 | 未確認 | `PoC_RAMScope_Main.vi`＋既知RAM変数 |
| `ReleaseBufferData`必須性 | 未確認 | 仕様確認＋A/B/C繰り返し試験 |
| APIスレッドセーフ性 | 未確認 | 仕様確認または排他前提で設計 |
| RAMデータEndian / Timestamp | 未確認 | SYSINFO・純正表示・既知値との比較 |
| CAN最終方式 | 未決定 | 要求、既存資産、タイミング、コストで選定 |

未確定事項は公開API内部へ固定せず、差し替え可能な薄いラッパと独立VIのまま検証する。

# 付録 A1. FG420 を用いた基盤単体試験 自動化手順（VI / TestStand）

別案件「**ファンクションジェネレータ FG420 を使った基盤単体試験用の自動化システム**」の
構築手順書（付録）。LabVIEW VI の作り方と TestStand シーケンスの設定手順を示す。

> 本付録は本編（[04](./04_VIの仕組み_LabVIEW基礎.md)〜[13](./13_構築ロードマップ.md)）の設計方針
> （1イベント1VI、入出力・エラー共通仕様、TestStand での条件管理）を**そのまま流用**する。
> 重複する基礎説明は本編を参照し、ここでは **FG420 固有の手順**に絞る。

> **本付録は実機 LabVIEW ドライバ前提（確定版）**：横河は FG410/FG420 用の **LabVIEW ドライバ**を
> 提供しており、その VI 群は **`IMFG410-63JA`（FG410/FG420 LabVIEW ドライバ ユーザーズマニュアル）**
> に記載されている。本付録の VI 名・手順は、このドライバ VI 一覧に基づく。
> → **SCPI を直接書く必要はなく、ドライバ VI を呼ぶだけ**でよい
> （ドライバ VI が内部で機器コマンドを処理する）。SCPI 直叩きは代替手段として A1.6 末尾に補足。

---

## A1.1 目的と対象

- **目的**：FG420 が出力する信号（正弦波・方形波・パルス等）を試験条件どおりに自動設定・出力し、
  **基盤（基板）単体の入出力特性を再現性高く試験**する。
- **対象機器**：横河 **FG420**（アービトラリ／ファンクションジェネレータ、**2 チャネル**）。
  - 兄弟機 FG410 は 1 チャネル。**FG420 は 2ch** のため、**チャネル指定**が要点（A1.4）。
  - 主な波形：正弦波・方形波・パルス・ランプ・各種パラメータ可変波形・ノイズ・DC・任意波形。
- **試験の基本形**：FG420 で **刺激信号を印加** → 基盤の応答を測定器（オシロ等）／基盤自身で取得 →
  TestStand で **合否判定**。試験条件を変えて再利用できるシーケンスにする。

---

## A1.2 機器構成とインタフェース

| 項目 | 内容 |
|------|------|
| 機器 | FG420（2ch ファンクションジェネレータ） |
| リモート I/F | **USB（USBTMC）** および **GP-IB**（標準のプログラマブル I/F） |
| 制御方法 | **横河提供 LabVIEW ドライバの VI を呼ぶ**（推奨・本付録の前提） |
| ドライバ | 横河 Web サイトから入手（`IMFG410-63JA` が当該ドライバのマニュアル） |
| VISA リソース例 | USB：`USB0::0x0B21::0x****::<SERIAL>::INSTR`（横河ベンダ ID は `0x0B21`）<br>GPIB：`GPIB0::<アドレス>::INSTR` |

> **Ethernet 可否は要確認**：FG400 系の標準リモートは USB / GP-IB。Ethernet 制御の可否は機器仕様で確認する。

---

## A1.3 環境構築（着手前の準備）

本編 [03_LabVIEW環境構築.md](./03_LabVIEW環境構築.md) と同じ要領で行う。FG420 固有点のみ記載。

1. **NI-VISA** をインストール（USBTMC / GPIB を VISA で扱う基盤）。
2. **FG420 の LabVIEW ドライバ**を横河サイトから入手し、`instr.lib` に配置
   （または VI Package Manager / 提供インストーラで導入）。本編 [03](./03_LabVIEW環境構築.md) 3.3 の
   バージョン互換性（古い対応表記でも新 LabVIEW で再コンパイルされる）も同様に適用。
3. **接続確認**：
   - USB なら NI MAX に USBTMC デバイスとして見えること、GPIB ならアドレスが見えることを確認。
   - `Open VISA Test Panel` で **`*IDN?`**（ドライバの `IDN` VI 相当）を送り、機器識別が返ることを確認。
   - VISA リソース名を記録（A1.2 の形式）。
4. ビット数（32/64bit）を LabVIEW・Runtime・ドライバで揃える。
5. ドライバ付属の **`VI Tree.vi`** を開くと、利用可能な全 VI が一覧配置されているので最初に確認する。

---

## A1.4 FG420 制御 VI の設計方針

本編 [05_VI設計方針と共通仕様.md](./05_VI設計方針と共通仕様.md) の共通仕様に準拠する。

### ドライバ VI の粒度 ＝ 本資料の「1イベント1VI」と相性が良い
- 本ドライバの VI は **「1 設定 = 1 VI」**（例：`FREQ`＝周波数だけ、`VOLT`＝振幅だけ）。
  これは本資料の **1イベント1VI** 方針とそのまま一致する。
- 実装の選択肢：
  - **(a) ドライバ VI を TestStand から直接呼ぶ**（最短）。
  - **(b) 共通 I/O（`Status.ctl` / `TestError.ctl`）を付けた薄いラッパ VI にする**
    （本編のエラー／ステータス運用に揃えたい場合）。
  - → 基盤試験では **(a) を基本**とし、判定・トレースを厚くしたい操作だけ (b) でラップするのが効率的。

### チャネル指定（FG420 = 2ch）
- チャネルの扱いはドライバの **`CHAN Mode`（チャネル・モード：独立／2 相 等）** で決める。
- 2ch を **同時に同設定**したい場合は **`INST Coup`（同時設定の選択）** を使う。
- 2ch の **周波数差／周波数比**を保つモードは **`CHAN Delt` / `CHAN Rat`**。
- **2ch の位相同期**は **`PHAS`（位相設定）＋ `PHAS Init`（位相同期実行）**。
- 各設定 VI のチャネル指定方法（VI 入力 or `CHAN Mode`/`INST Coup` 連動）は、
  ドライバ各 VI のコネクタ（入出力端子）で確認する。

### 共通方針
- **試験条件は TestStand 側で変数管理**（波形・周波数・振幅などを変数化）。
- **内部待ち時間は VI に埋め込まない**（同期／待ちは TestStand 側、本編 [11](./11_TestStandシーケンス構築手順.md)）。

---

## A1.5 使用する主なドライバ VI（基盤試験で常用）

`IMFG410-63JA` の VI 一覧から、基盤単体試験で使う代表 VI を用途別に抜粋。

### 接続・共通（IEEE488.2）
| 用途 | ドライバ VI |
|------|-------------|
| 接続・初期化（内部で `*RST`） | **`Initialize`** |
| 終了（VISA セッションを閉じる） | **`Close`** |
| 機器 ID 確認 | **`IDN`**（`*IDN?`） |
| リセット | **`RST`**（`*RST`） |
| 完了同期 | **`OPC`** / **`WAI`** |
| 自己診断 | **`TST`**（`*TST?`） |
| エラー照会 | **`SYST Err`** |
| 設定メモリクリア | **`MEM Del`** |

### 基本出力設定（最頻出）
| 用途 | ドライバ VI | 備考 |
|------|-------------|------|
| 波形種別 | **`FUNC`** | 正弦/方形/パルス/ランプ等 |
| 任意波形の選択 | **`FUNC User`** | 任意波使用時 |
| 周波数 | **`FREQ`**（単位 `FREQ Unit` / `FREQ User`） | |
| 振幅（出力レベル） | **`VOLT`** | 振幅設定。Hi-Lo 指定は `VOLT High` / `VOLT Low`、レンジ `VOLT Rang`、範囲 `OUTP Scal` |
| DC オフセット | **`VOLT Offs`** | |
| 位相 | **`PHAS`** / 位相同期 **`PHAS Init`** | 2ch 位相関係 |
| **出力 ON/OFF** | **`OUTP`** | 基盤への印加開始/停止 |
| 出力極性 | **`OUTP Pol`** | |
| 負荷インピーダンス | **`OUTP Load`** | 50Ω / Hi-Z 等の整合 |
| 電源投入時の出力状態 | **`OUTP Pon`** | **安全のため OFF 推奨**（A1.9） |

### チャネル（2ch）
`CHAN Mode`, `CHAN Delt`, `CHAN Rat`, `INST Coup`（A1.4）。

### バースト・トリガ（必要時）
`BURS Mode` / `BURS Ncyc` / `BURS Stat` / `BURS Tdel` / `BURS Spac` / `BURS Slev`、
`TRIG`（トリガ発行）/ `TRIG Sel` / `TRIG Sour` / `TRIG Slop` / `TRIG Tim`、
複数台同期は `ROSC Sour`（基準周波数源）。

### 変調・スイープ・任意波/シーケンス（高度な試験時）
- 変調：`AM*` `AMSC*` `FM*` `PM*` `PWM*` `FSK*` `PSK*` `OFSM*`（各 Freq/Func/Sour/Stat）。同期出力 `OUTP <変調>`。
- スイープ：`FREQ*` `AMPL*` `PHAS*` `PULS*` `OFFS*`（Mode/Star/Stop/Span/Stat/Swap）、`SWE *`、`MARK *`。
- 任意波/シーケンス：`TRAC Plot/XY/Rec/Star/Copy/Del/Inf`、`SEQ` 系、`TRIG Comp`（コンパイル）。
- ステータス監視：`OPER*` `QUES*` `WARN*` `STAT Pres` `CLS` `ESE` `ESR` `SRE` `STB`。

---

## A1.6 VI の作り方（ドライバ利用）

本編 [06_VIの作り方_手順.md](./06_VIの作り方_手順.md) をベースに、FG420 では**ドライバ VI を組み合わせる**。

### 基本パターン（ブロックダイアグラム）
```
Initialize ─▶(refnum/error)─▶ FUNC ─▶ FREQ ─▶ VOLT ─▶ VOLT Offs ─▶ OUTP(ON) ─▶ … ─▶ OUTP(OFF) ─▶ Close
            （VISAリファレンスと error in/out を数珠つなぎ：実行順序を確定。本編 04 のデータフロー）
```
- ドライバ VI は **VISA リファレンスと標準の error in/out** を持つので、本編 [04](./04_VIの仕組み_LabVIEW基礎.md)
  4.4/4.6 のとおり**リファレンスとエラーを引き回す**だけで順序とエラー伝搬が成立する。

### 薄いラッパ VI にする場合（任意）
1. テンプレート VI（本編 6.6）を複製。
2. 該当ドライバ VI（例：`FREQ`）を配置し、入力（ch・条件値）を配線。
3. ドライバの error out を `TestError.ctl`（機器名 `FG420`）/ `Status.ctl` に変換。
4. コネクタペインを共通の並びで割り当て（本編 6.4）。
   - 推奨ラッパ：`FG420_Init`(=Initialize) / `FG420_Set_Func`(=FUNC) / `FG420_Set_Freq`(=FREQ) /
     `FG420_Set_Ampl`(=VOLT) / `FG420_Set_Offset`(=VOLT Offs) / `FG420_Output`(=OUTP) /
     `FG420_Set_Phase`(=PHAS,PHAS Init) / `FG420_Close`(=Close)。

### 補足：SCPI 直叩き（ドライバを使わない場合のみ）
ドライバが使えない環境では VISA で SCPI を送る（本編 [06](./06_VIの作り方_手順.md) 方式B）。
**ドライバが提供されている本案件では不要**。必要時のみ、ドライバの各 VI 名（`FUNC`/`FREQ`/`VOLT`/`OUTP` 等）に
対応する機器コマンドを `IMFG410-63JA`／機器マニュアルで確認して用いる。

---

## A1.7 TestStand シーケンス構築手順

本編 [11_TestStandシーケンス構築手順.md](./11_TestStandシーケンス構築手順.md) に準拠。

1. **LabVIEW Adapter** を設定し、ドライバ VI（またはラッパ VI）を呼べるようにする。
2. **試験条件を変数化**（FileGlobals / Parameters）：
   - `波形種別`、`周波数`、`振幅`、`オフセット`、`位相`、`チャネル`、`負荷Z`、`判定しきい値` 等。
   - 条件を CSV / プロパティファイルで管理し、`Property Loader` で読み込むと条件変更が容易。
3. **イベント単位のサブシーケンス化**（本編 11.5）：
   - `SubSeq_FG設定`（`FUNC`→`FREQ`→`VOLT`→`VOLT Offs`→（`PHAS`/`PHAS Init`）→（`OUTP Load`））
   - `SubSeq_出力ON`（`OUTP` = ON）
   - `SubSeq_応答測定`（オシロ等で取得、または基盤からの読み出し）
   - `SubSeq_判定`
   - `SubSeq_出力OFF`（`OUTP` = OFF）
4. **MainSequence** で `Sequence Call` により順序を構成。条件で並び替え・流用。
5. **合否判定**：測定値と `判定しきい値` を比較し、ステップ Status（Pass/Fail）を設定。
   エラーは `SYST Err`／ステータスレジスタ（`OPER*`/`QUES*`）で確認。
6. **同期／非同期・待ち**は TestStand 側で明示（出力安定待ちは `Wait`、設定確定は `OPC`/`WAI`）。
7. **レポート**：試験条件・設定値・測定値・合否を記録（本編 11.7）。

---

## A1.8 基盤単体試験の典型フロー（例）

```
Setup    : Initialize（*IDN?確認） / OUTP Pon=OFF / 測定器Init / 条件ロード
Main
  ├ SubSeq_FG設定: FUNC(波形) → FREQ(周波数) → VOLT(振幅) → VOLT Offs(オフセット)
  │               → (OUTP Load 整合) → (2ch時: CHAN Mode / PHAS / PHAS Init)
  ├ SubSeq_出力ON: OUTP(ON)   （+ 必要なら TRIG でバースト/単発発行）
  ├ Wait（出力・基盤の安定待ち：時間は条件変数）
  ├ SubSeq_応答測定（オシロ波形 / 基盤の出力値取得）
  ├ SubSeq_判定（測定値 vs しきい値 → Pass/Fail）
  └ SubSeq_出力OFF: OUTP(OFF)
Cleanup  : OUTP(OFF)（全ch） / Close / 測定器Close   ← 異常時も必ず実行
```

- 複数条件（周波数スイープ等）は、条件配列で **For ループ** で回すか、機器内蔵スイープ（`FREQ*`/`SWE*`）を使う。
- 2ch を使う試験は、`CHAN Mode` を設定のうえ ch1/ch2 を設定・`OUTP`、必要なら `PHAS Init` で位相同期。

---

## A1.9 異常系・安全

本編 [12_異常系処理とシャットダウン設計.md](./12_異常系処理とシャットダウン設計.md) の考え方を適用。

- **Cleanup で必ず `OUTP`=OFF（全ch）→ `Close`** を実行（異常終了でも出力を残さない）。
- **`OUTP Pon`=OFF** に設定（電源投入時に出力 OFF＝予期せぬ印加を防止）。
- 基盤に過大入力を与えないよう、**振幅・オフセットの上限を試験条件側でガード**（範囲チェック）。
- **`OUTP Load`（負荷インピーダンス）を基盤条件に合わせる**（不整合で実効レベルがずれる）。
- 異常時のエラー（機器名 `FG420`・`SYST Err` 内容）を `TestError.ctl` で TestStand へトレース。

---

## A1.10 構築ステップ（チェックリスト）

- [ ] FG420 ドライバ入手・導入、`VI Tree.vi` 確認、`Initialize`→`IDN`で通信確認（A1.3）
- [ ] 使う VI を確定（A1.5）。必要な操作だけ薄いラッパ VI 化（A1.6）
- [ ] 各 VI の**コネクタ（チャネル入力の有無等）**を `IMFG410-63JA` の該当ページで確認
- [ ] 共通型定義（`Status.ctl` / `TestError.ctl`）は本編のものを流用
- [ ] 単体確認（`FUNC`/`FREQ`/`VOLT`/`VOLT Offs`/`OUTP`、2ch時は `CHAN Mode`/`PHAS Init`）
- [ ] TestStand：条件変数化・サブシーケンス化・判定・Cleanup（`OUTP` OFF）
- [ ] 1 条件で End-to-End 実行 → 条件変更で再利用できることを確認

---

## A1.11 参考資料

- **`IMFG410-63JA`（FG410/FG420 LabVIEW ドライバ ユーザーズマニュアル）** ← 本付録の VI 名・手順の根拠
- FG410/FG420 ユーザーズマニュアル（基本／通信）：`IMFG410-01EN` / `IMFG410-02EN` / `IMFG410-64EN`（横河）
- FG420 LabVIEW / LabWindows/CVI ドライバ：横河 Test&Measurement サイトのダウンロードページ

---

## A1.12 残課題（着手時に確認）

- 各ドライバ VI の **コネクタ（入出力端子）詳細**、特に **チャネル指定の渡し方**
  （VI 入力か `CHAN Mode`/`INST Coup` 連動か）。`IMFG410-63JA` の各 VI 個別ページで確定。
- 振幅指定の単位系（`VOLT`＝Vpp/振幅か、`VOLT High`/`Low`＝Hi-Lo か）の使い分け。
- **Ethernet 制御の可否**（標準は USB / GP-IB）。
- 任意波形／シーケンス運用が必要なら `TRAC*` / `SEQ*` / `TRIG Comp` の使用フロー確定。

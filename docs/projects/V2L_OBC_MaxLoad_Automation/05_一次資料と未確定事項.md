# 05. 一次資料と未確定事項

**最終整理日:** 2026-09-03  
**State:** Living document

---

## 1. Source Authority

本Projectでも[`../../00C_一次資料とバージョン基準.md`](../../00C_一次資料とバージョン基準.md)を適用する。

優先順位は原則次。

1. 対象PCへ実際に導入するDriver Help / Type Library / Header / DLL / proto
2. 対象Versionのメーカー公式API仕様書・Manual
3. メーカー公式Sample
4. 実機PoC結果
5. 本Project設計
6. Web調査・Community情報

Web/Community情報はAPIの存在を探す入口には利用するが、Native Method名、引数、型、制限値を確定する正本にはしない。

---

## 2. 入手済みProject資料

以下は2026-09-03時点で調査に使用した資料。ライセンス/再配布条件が未確認のVendor binary/manualは、このProject directoryへ勝手にコピーしない。

| Source | 内容 | 状態 |
|---|---|---|
| 最大負荷試験構成図 | Chroma 62180D/63206A/63804/61830系とDUT接続 | 入手済み |
| `Xin1結線図案(1).xlsx` | 恒温槽、冷却、電源/負荷、Power Analyzer、CANalyzer、INCA等を含む設備接続案 | 入手済み |
| `IMD037-01JA_090.pdf` / `IMD037-01EN_090.pdf` | Yokogawa IS8000 API User's Manual | 入手済み |
| `IS8000SDK.dll` | IS8000 User Library API assembly | 入手済み |
| `IS8000SDKHelp_ja.chm` | IS8000 User Library API Help | 入手済み |
| `DataAcquisitionServiceTemplate.zip` | IS8000 DAQ User Library template | 入手済み |
| `FileServiceTemplate.zip` | IS8000 Waveform File User Library template | 入手済み |
| `Sample.zip` | IS8000 SDK sample | 入手済み |
| `IS8000Control.zip` | gRPC Help / proto / sample | 入手済み |
| `RAMScopeVP_API_Specification(1).pdf` | RAMScope-EX/EXG Hardware Control API Rev.5.0 | 入手済み |
| `INCA Tool-API Documentation.chm` | ETAS INCA Tool API正本候補 | 入手済み |
| CANalyzer 12 Type Library | 既存本編でLabVIEWから確認済み | 既存資産 |

---

# 3. IS8000で確認済みの設計事実

## 3.1 APIが2系統

**確定:**

- User Library: `IS8000SDK.dll`を用いてIS8000へ新しいDAQ Source/File Sourceを追加する。
- Control API: 外部ApplicationからgRPCでIS8000を操作する。Control Application作成に`IS8000SDK.dll`は不要。

この境界を設計正本とする。

## 3.2 User Library開発条件

入手した現行ManualではUser LibraryはC#/.NET 8/64bitを前提とする。

## 3.3 Control API

入手済みproto/help/sampleで、Device/DataSource、Tab、Monitor、Record、SaveFolder、Waveform、Project等のAPIが確認できている。

**未確定:**

- `MDF` recordingの実際のMDF version / `.mf4`可否
- File nameを外部から直接指定できるか
- Recording artifact生成完了を確実に判定する最終手順
- Timestampの絶対時刻Source

---

# 4. RAMScopeで確認済みの設計事実

入手したRev.5.0 API仕様書では以下を確認済み。

- Target MCU RAM Read/Write機能がAPI基本機能に含まれる。
- `RAMScopeGT150MemoryRead/Write`
- `RAMScopeGT150ContinualyMemoryRead/Write`
- GT17x `RAMScopeGT170ScenarioWriteStart/Stop`
- Memory Read/WriteはMeasurement中にも利用可能。
- Measurement中のMemory Read/Writeは測定周期末に実行される。
- Scenario WriteはPRO License対象。
- Packet TimestampはMeasurement startを0とする20 ns countとして記載されている。

## 4.1 bitness競合

**Critical open issue:**

Rev.5.0 PDFは32bit native/WOW64と記載する一方、既存本編は対象環境で`RAMScopeVP_API_x64.dll`を採用している。

Projectでは次を実機/実ファイルで再確認する。

```text
RAMScope I.K. Version
RAMScopeVP_API DLL file name
PE bitness
Header revision
Export symbols
Runtime dependencies
```

既存本編のx64実装を、古いPDF記載だけを理由に変更しない。

---

# 5. CANalyzerで確認済みの設計事実

既存本編のCANalyzer 12 Type Library確認結果を流用する。

**Confirmed model:**

```text
Application → IApplication10
System → ISystem3
Measurement → IMeasurement5
Version → IVersion2
Configuration → IConfiguration16
```

Measurement Start/Stop、Running、Configuration Open、SysVar Read/Write、Version等のWrapper資産が存在する。

**Project open issue:**

- Recorder Start/Stop API
- Recording artifact path
- MF4保存条件
- Sync/Event Marker
- CAN hardware timestampとMF4 timebase

対象PCに導入するCANalyzer VersionのType Library/COM Helpを再確認して確定する。

---

# 6. INCAで確認済み/未確認

`INCA Tool-API Documentation.chm`を入手済み。

**設計上必要な機能:**

- Application connect
- Experiment/Workspace/Device context
- Hardware initialize
- Measurement start/stop
- selected measurement read
- Calibration read/write/readback
- Recorder start/stop
- Recording metadata/header
- Event/Sync marker
- Recording artifact path
- Version/Cleanup

**未確定:**

現時点のProject文書ではCHMのNative member名を完全抽出していないため、外注仕様Freeze前にCHMまたは対象PCのCOM/.NET Type情報でSymbol/Signatureを監査する。

確認時は次を記録する。

```text
Class / Interface
Method / Property
Input type
Output type
Exception / Error behavior
Version introduced/deprecated
Recorder file format
Threading / process restriction
```

---

# 7. WT5000未確定事項

- 対象PCへ導入するLabVIEW Driver package
- Driver VI名/Connector Pane
- Communication Interface Manual revision
- numeric/harmonic/waveform read API
- external trigger/remote trigger仕様
- sample timestamp/timebase
- Driver/Instrument bitness制約

Driverを入手したらProject `02`の仮APIを実VI名へマッピングする。

---

# 8. MX100未確定事項

- MX100/DARWIN API packageの実DLL名
- DLL bitness
- Function signature
- Channel configuration model
- scan interval
- timestamp source
- hardware trigger/clock sync
- long-duration streaming behavior

API package、header、sampleを入手後に確定する。

---

# 9. Chroma未確定事項

各Modelについて次を確認する。

```text
Official LabVIEW / IVI Driver availability
Driver version
Remote Programming Manual revision
VISA transport
SCPI command set
Parallel operation
Trigger / sequence capability
Fault/protection behavior
Output-off behavior at communication loss
```

特に`62180D-600D ×2`の最大負荷運転は単純な2Session同時Setpoint操作として扱わず、メーカーが定めるParallel/Master-Slave条件を確認する。

---

# 10. System-level Open Issues

| ID | Issue | Priority |
|---|---|---:|
| SYS-001 | Software Triggerだけで必要同期精度を満たせるか | High |
| SYS-002 | Hardware trigger可能機器と端子/信号仕様 | High |
| SYS-003 | IS8000 MDFがMF4か | Medium |
| SYS-004 | 最終的にSingle MF4へMergeする必要性/方法 | Medium |
| SYS-005 | CANalyzer/INCA/IS8000間の共通Marker方式 | High |
| SYS-006 | RAMScope bitnessとIS8000 User Library統合方式 | Critical |
| SYS-007 | Recorder artifactのflush完了判定 | High |
| SYS-008 | xlsx条件定義のSchemaとTestStand Property Loader利用範囲 | Medium |
| SYS-009 | DUT/設備Emergency StopとSoftware Cleanup境界 | Critical |
| SYS-010 | 高帯域DAQを同一PCへ集約した場合のCPU/Disk/USB/Ethernet負荷 | High |

---

# 11. 次の調査順

1. RAMScope対象環境の実DLL bitness/Version確定
2. INCA CHM / Type LibraryのNative API inventory
3. WT5000 LabVIEW Driver + Communication Manual
4. MX100 API DLL/header/sample
5. Chroma各Model Driver/Programming Manual
6. CANalyzer Recorder/Marker API
7. IS8000実機でMDF/MF4、file naming、notificationをPoC
8. Cross-recorder Sync PoC

調査結果を得たらProject `02`のAPI要求表と`04`のAcceptance Criteriaを先に更新する。
